# Environment
.env
*.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Data files — never commit generated data to main branch
# These live on gh-pages only
data/app/catalogue.json
data/app/stock.json
data/app/velocity.json
data/app/reps.json
data/app/token.json

# The rep CSV stays on main (it IS source of truth)
# but do NOT gitignore it — it must be committed

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
